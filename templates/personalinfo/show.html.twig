{% extends 'base.html.twig' %}

{% block title %}Personalinfo{% endblock %}

{% block body %}
    <h1>Personalinfo</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ personalinfo.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ personalinfo.name }}</td>
            </tr>
            <tr>
                <th>Phone</th>
                <td>{{ personalinfo.phone }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ personalinfo.email }}</td>
            </tr>
            <tr>
                <th>Presentation</th>
                <td>{{ personalinfo.presentation }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('personalinfo_index') }}">back to list</a>

    <a href="{{ path('personalinfo_edit', {'id': personalinfo.id}) }}">edit</a>

    {{ include('personalinfo/_delete_form.html.twig') }}
{% endblock %}
